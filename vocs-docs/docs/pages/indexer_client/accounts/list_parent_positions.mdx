### List Parent Positions

:::code-group

```rust [Rust]
let opts = ListPositionOpts {
    status: Some(PerpetualPositionStatus::Open),
    limit: Some(5),
    created_before_or_at_height: None,
    created_before_or_at: None,
};
let response_list = accounts.list_parent_positions(&parent_subaccount, Some(opts)).await?;
```

```python [Python]

```

```typescript [TypeScript]

```

:::



