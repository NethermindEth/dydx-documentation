### Get Perpetual Markets

Get information of perpetual market

#### Method Declaration

:::code-group

```python [Python]
def get_perpetual_markets(self, market: str = None) -> Response
```

```typescript [TypeScript]
async getPerpetualMarkets(market?: string): Promise<Data>
```

```rust [Rust]
pub async fn list_perpetual_markets(
    &self,
    opts: Option<ListPerpetualMarketsOpts>,
) -> Result<HashMap<Ticker, PerpetualMarket>, Error>
```

```url [API]
/v4/perpetualMarkets
```

:::

:::details[Unification Plan]
:::

#### Parameters

:::details[Optional]

| Parameter                     | Location | Type                      | Description                                                  |
| ----------------------------- | -------- | ------------------------- | ------------------------------------------------------------ |
| `limit`                       | query    | [u32]                     | Maximum number of asset positions to return in the response. |
| `ticker` | query | [Ticker] | |

#### Response

| Status | Meaning     | Schema                          |
| ------ | ----------- | ------------------------------- |
| `200`  | [OK]        | Key value pair of [Ticker] -> [PerpetualMarket] ⛁ |

#### Method Usage

:::code-group

```rust [Rust]
let opts = ListPerpetualMarketsOpts {
    limit: Some(5),
    ticker: Some(ticker),
};
let response = markets.list_perpetual_markets(Some(opts)).await?;
```

```python [Python]

```

```typescript [TypeScript]

```

:::

[OK]: /types/ok
[u32]: /types/u32
[Ticker]: /types/address
[PerpetualMarket]: /types/perpetual_market