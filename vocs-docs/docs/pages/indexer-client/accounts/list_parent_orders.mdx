### List Parent Orders

Query for orders filtered by order params of a parent subaccount.

#### Method Declaration

:::code-group

```python [Python]

```

```typescript [TypeScript]
async getParentSubaccountNumberOrders(
    address: string,
    parentSubaccountNumber: number,
    ticker?: string | null,
    side?: OrderSide | null,
    status?: OrderStatus | null,
    type?: OrderType | null,
    limit?: number | null,
    goodTilBlockBeforeOrAt?: number | null,
    goodTilBlockTimeBeforeOrAt?: string | null,
    returnLatestOrders?: boolean | null,
): Promise<Data>
```

```rust [Rust]
pub async fn list_parent_orders(
    &self,
    subaccount: &ParentSubaccount,
    opts: Option<ListOrdersOpts>,
) -> Result<ListOrdersResponse, Error>
```

```url [API]
/v4/orders/parentSubaccountNumber
```

:::

:::details[Unification Plan]
:::

#### Parameters

| Parameter                     | Location  | Type                  | Description                                                           |
| ----------------------------- | --------- | --------------------- | --------------------------------------------------------------------- |
| `address`                     | query     | [Address]             | The wallet address that owns the subaccount.                          |
| `parent_subaccount_number`    | query     | [SubaccountNumber]    |                                                                       |

:::details[Optional]

| Parameter                             | Location  | Type              | Description                                                   |
| ------------------------------------- | --------- | ----------------- | ------------------------------------------------------------- |
| `limit`                               | query     | [u32]             | Maximum number of asset positions to return in the response.  |
| `ticker`                              | query     | [Ticker]          | The ticker filter.                                            |
| `side`                                | query     | [OrderSide]       | The order side filter.                                        |
| `status`                              | query     | [OrderStatus]     | The order status filter.                                      |
| `type`                                | query     | [OrderType]       | The order type filter.                                        |
| `good_til_block_before_or_at`         | query     | [Height]          | The block number filter for orders good until before or at.   |
| `good_til_block_time_before_or_at`    | query     | [DateTime in utc] | The timestamp filter for orders good until before or at.      |
| `return_latest_orders`                | query     | bool              | Whether to return only the latest orders.                     |

:::

#### Response

| Status | Meaning     | Schema                         |
| ------ | ----------- | ------------------------------ |
| `200`  | [OK]        | [ListOrdersResponse]           |

[Address]: /types/address
[SubaccountNumber]: /types/subaccount_number
[u32]: /types/u32
[Ticker]: /types/ticker
[OrderSide]: /types/order_side
[OrderStatus]: /types/order_status
[OrderType]: /types/order_type
[Height]: /types/height
[DateTime in utc]: /types/date_time
[ListOrdersResponse]: /types/list_orders_response
[OK]: /types/ok