### Get Order

Retrieves detailed information about a specific order based on its unique identifier.

This method allows clients to fetch the details of a particular order from the dYdX exchange by providing its order ID. The method sends a GET request to the `/v4/orders/{orderId}` endpoint and returns the order data.

#### Method Declaration

:::code-group

```python [Python]
def get_order(
    self,
    order_id: str
) -> Response
```

```typescript [TypeScript]
async getOrder(orderId: string): Promise<Data>
```

```rust [Rust]
pub async fn get_order(
    &self,
    order_id: &OrderId,
) -> Result<OrderResponseObject, Error>
```

```url [API]
/v4/orders/{order_id}
```

:::

:::details[Unification Plan]
:::

#### Parameters

| Parameter           | Location | Type               | Description                                                           |
| ------------------- | -------- | ------------------ | --------------------------------------------------------------------- |
| `address`           | query    | [Address]          | The wallet address that owns the subaccount.                          |

:::details[Optional]

| Parameter                     | Location | Type                      | Description                                                  |
| ----------------------------- | -------- | ------------------------- | ------------------------------------------------------------ |
| `limit`                       | query    | [u32]                     | Maximum number of asset positions to return in the response. |

#### Response

| Status | Meaning     | Schema                          |
| ------ | ----------- | ------------------------------- |
| `200`  | [OK]        | [AssetPositionResponseObject] ⛁ |

#### Method Usage

:::code-group

```python [Python]

```

```typescript [TypeScript]

```

```rust [Rust]
let response = accounts.get_order(&order_id).await?;
```

:::



