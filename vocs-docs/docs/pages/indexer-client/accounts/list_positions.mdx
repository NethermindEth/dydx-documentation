### List Positions

// TODO: Add description

#### Method Declaration

:::code-group

```rust [Rust]
pub async fn list_positions(
    &self,
    subaccount: &Subaccount,
    opts: Option<ListPositionsOpts>,
) -> Result<Vec<PerpetualPositionResponseObject>, Error>
```

```python [Python]
def get_subaccount_perpetual_positions(
    self,
    address: str,
    subaccount_number: int,
    status: str = None,
    limit: int = None,
    created_before_or_at_height: int = None,
    created_before_or_at_time: str = None,
) -> Response
```

```typescript [TypeScript]
async getSubaccountPerpetualPositions(
    address: string,
    subaccountNumber: number,
    status?: PositionStatus | null,
    limit?: number | null,
    createdBeforeOrAtHeight?: number | null,
    createdBeforeOrAt?: string | null,
): Promise<Data> 
```

```url [API]
/v4/perpetualPositions
```

:::

#### Parameters

| Parameter | Location | Type  | Required | Description |
| --------- | -------- | ----- | -------- | ----------- |
| address | param | [Address] | true | |
| subaccountNumber | param | [SubaccountNumber] | true | |
| status | param | [PositionStatus] | false | |
| limit | param | int | false | |
| createdBeforeOrAtHeight | param | int | false | |
| createdBeforeOrAt | param | int | false | |

#### Response

| Status | Meaning | Description | Schema |
| ------ | ------- | ----------- | ------ |
| 200    | [OK]    | Ok          | [PerpetualPositionResponseObject] |

#### Method Usage

:::code-group

```rust [Rust]
let opts = ListPositionOpts {
    status: Some(PerpetualPositionStatus::Open),
    limit: Some(5),
    created_before_or_at_height: None,
    created_before_or_at: None,
};
let response_list = accounts.list_positions(&subaccount, Some(opts)).await?;
```

```python [Python]

```

```typescript [TypeScript]

```

:::

[Address]: /types/address
[SubaccountNumber]: /types/subaccount_number
[PositionStatus]: /types/position_status
[OK]: https://datatracker.ietf.org/doc/html/rfc7231#section-6.3.1
[PerpetualPositionResponseObject]: /types/perpetual_position_response_object

