### Subaccounts

Data feed of a subaccount. Data contains updates to the subaccount such as position, orders and fills updates.

#### Method Declaration

:::code-group

```python [Python]
# class `Subaccounts`
def subscribe(self, address: str, subaccount_number: int) -> Self
def unsubscribe(self, address: str, subaccount_number: int)
```

```typescript [TypeScript]
// class `IndexerSocket`
subscribeToSubaccount(address: string, subaccountNumber: number): void
unsubscribeFromSubaccount(address: string, subaccountNumber: number): void
```

```rust [Rust]
// struct `Feeds`
pub async fn subaccounts(
    &mut self,
    subaccount: Subaccount,
    batched: bool,
) -> Result<Feed<SubaccountsMessage>, FeedError>
// The stream is unsubscribed when the `Feed` object is dropped
```

```url [Channel]
v4_subaccounts
```

:::

:::details[Unification Plan]
:::

#### Parameters

##### Subscribe

| Parameter           | Location | Type               | Description                                                           |
| ------------------- | -------- | ------------------ | --------------------------------------------------------------------- |
| `id`                | query    | string             | The subaccount ID formatted as `{address}/{subaccount-number}.` |
| `batched`           | query    | bool               | Reduce incoming messaging by batching contents. |

##### Unsubscribe

| Parameter           | Location | Type               | Description                                                           |
| ------------------- | -------- | ------------------ | --------------------------------------------------------------------- |
| `id`                | query    | string             | The subaccount ID formatted as `{address}/{subaccount-number}.`       |

#### Data

##### Initial message

| Status | Meaning     | Schema                          |
| ------ | ----------- | ------------------------------- |
| `200`  | [OK]        | [SubaccountsInitialMessage]     |

##### Update message

| Status | Meaning     | Schema                          |
| ------ | ----------- | ------------------------------- |
| `200`  | [OK]        | [SubaccountsUpdateMessage]      |

[address]: /types/address
[subaccount-number]: /types/subaccount_number
