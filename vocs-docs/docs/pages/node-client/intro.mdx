# Node Client

Connect to a dYdX Full Node.

:::code-group

```python [Python]
@staticmethod
async def connect(config: NodeConfig) -> Self
```

```typescript [TypeScript]
static async connect(network: Network): Promise<CompositeClient>
```

```rust [Rust]
let node_client = NodeClient::connect(config);
```

:::
