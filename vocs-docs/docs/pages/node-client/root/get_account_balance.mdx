### Get Account Balance

        """
        Retrieves the account balance for a specific denomination.

        Args:
            address (str): The account address.
            denom (str): The denomination of the balance.

        Returns:
            bank_query.QueryBalanceResponse: The response containing the account balance.
        """

Query for account [balance] by address for a given denomination.

[balance]: https://github.com/cosmos/cosmos-sdk/tree/main/x/bank#balance

#### Method Declaration

:::code-group

```python [Python]
async def get_account_balance(
    self,
    address: str,
    denom: str,
) -> bank_query.QueryBalanceResponse
```

```typescript [TypeScript]
async getAccountBalance(
    address: string,
    denom: string,
): Promise<Coin | undefined>
```

```rust [Rust]
pub async fn get_account_balance(
    &mut self,
    address: &Address,
    denom: &Denom,
) -> Result<Coin, Error>
```

```url [API]
```

:::

:::details[Unification Plan]
:::

#### Parameters

| Parameter           | Location | Type               | Description                                                           |
| ------------------- | -------- | ------------------ | --------------------------------------------------------------------- |
| `address`           | query    | [Address]          | The wallet address that owns the subaccount.                          |

:::details[Optional]

| Parameter                     | Location | Type                      | Description                                                  |
| ----------------------------- | -------- | ------------------------- | ------------------------------------------------------------ |
| `limit`                       | query    | [u32]                     | Maximum number of asset positions to return in the response. |

#### Response

| Status | Meaning     | Schema                          |
| ------ | ----------- | ------------------------------- |
| `200`  | [OK]        | [AssetPositionResponseObject] ⛁ |

#### Method Usage

:::code-group

```rust [Rust]
```

```python [Python]
response = await node_client.get_account_balances(TEST_ADDRESS)
```

```typescript [TypeScript]
const client = new CompositeClient(network);
wallet = await LocalWallet.fromMnemonic(DYDX_TEST_MNEMONIC, BECH32_PREFIX);
const tx = await client.validatorClient.get.getAccountBalance(
    wallet.address,
    client.validatorClient.config.denoms.USDC_DENOM,
);```

:::

Examples: [Python] | [Rust]

[Python]: https://github.com/dydxprotocol/v4-clients/blob/3e8c7e1b960291b7ef273962d374d9934a5c4d33/v4-client-py-v2/examples/validator_get_example.py#L28
[Rust]: https://github.com/dydxprotocol/v4-clients/blob/3e8c7e1b960291b7ef273962d374d9934a5c4d33/v4-client-rs/client/examples/validator_get.rs#L38
