### Get Clob Pair

:::details[TODO: Check]
        """
        Retrieves a CLOB pair by its ID.

        Args:
            pair_id (int): The CLOB pair ID.

        Returns:
            clob_pair_type.ClobPair: The CLOB pair.
        """
:::

Query for the orderbook pair by its id.

#### Method Declaration

:::code-group

```python [Python]
async def get_clob_pair(self, pair_id: int) -> clob_pair_type.ClobPair
```

```typescript [TypeScript]
async getClobPair(pairId: number): Promise<ClobModule.QueryClobPairResponse>
```

```rust [Rust]
pub async fn get_clob_pair(&mut self, pair_id: u32) -> Result<ClobPair, Error>
```

```url [API]
```

:::

:::details[Unification Plan]
:::

#### Parameters

| Parameter           | Location | Type               | Description                                                           |
| ------------------- | -------- | ------------------ | --------------------------------------------------------------------- |
| `address`           | query    | [Address]          | The wallet address that owns the subaccount.                          |

:::details[Optional]

| Parameter                     | Location | Type                      | Description                                                  |
| ----------------------------- | -------- | ------------------------- | ------------------------------------------------------------ |
| `limit`                       | query    | [u32]                     | Maximum number of asset positions to return in the response. |

#### Response

| Status | Meaning     | Schema                          |
| ------ | ----------- | ------------------------------- |
| `200`  | [OK]        | [AssetPositionResponseObject] ⛁ |

#### Method Usage

:::code-group

```rust [Rust]
```

```python [Python]
network = Network.testnet()
client = ValidatorClient(network.validator_config)
clob_pair = client.get.clob_pair(1)
```

```typescript [TypeScript]
const client = await ValidatorClient.connect(Network.testnet().validatorConfig);
const clobpair = await client.get.getClobPair(1);
```

:::

Examples: [Python] | [Rust]

[Python]: https://github.com/dydxprotocol/v4-clients/blob/3e8c7e1b960291b7ef273962d374d9934a5c4d33/v4-client-py-v2/examples/validator_get_example.py#L86
[Rust]: https://github.com/dydxprotocol/v4-clients/blob/3e8c7e1b960291b7ef273962d374d9934a5c4d33/v4-client-rs/client/examples/validator_get.rs#L71
