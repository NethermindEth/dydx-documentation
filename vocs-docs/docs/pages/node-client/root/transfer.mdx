### Transfer

:::details[TODO: Check]
        """
        Transfers funds between subaccounts.

        Args:
            wallet (Wallet): The wallet to use for signing the transaction.
            sender_subaccount (SubaccountId): The sender subaccount ID.
            recipient_subaccount (SubaccountId): The recipient subaccount ID.
            asset_id (int): The asset ID.
            amount (int): The amount to transfer.

        Returns:
            The response from the transaction broadcast.
        """
:::

Transfer funds (USDC) between subaccounts.

#### Method Declaration

:::code-group

```python [Python]
async def transfer(
    self,
    wallet: Wallet,
    sender_subaccount: SubaccountId,
    recipient_subaccount: SubaccountId,
    asset_id: int,
    amount: int,
)
```

```typescript [TypeScript]
TODO: transfer
```

```rust [Rust]
pub async fn transfer(
    &mut self,
    account: &mut Account,
    sender: Subaccount,
    recipient: Subaccount,
    amount: impl Into<Usdc>,
) -> Result<TxHash, NodeError>
```

```url [API]
```

:::

:::details[Unification Plan]
:::

#### Parameters

| Parameter           | Location | Type               | Description                                                           |
| ------------------- | -------- | ------------------ | --------------------------------------------------------------------- |
| `address`           | query    | [Address]          | The wallet address that owns the subaccount.                          |

:::details[Optional]

| Parameter                     | Location | Type                      | Description                                                  |
| ----------------------------- | -------- | ------------------------- | ------------------------------------------------------------ |
| `limit`                       | query    | [u32]                     | Maximum number of asset positions to return in the response. |

:::

#### Response

| Status | Meaning     | Schema                          |
| ------ | ----------- | ------------------------------- |
| `200`  | [OK]        | [AssetPositionResponseObject] ⛁ |

#### Method Usage

:::code-group

```rust [Rust]
```

```python [Python]
network = Network.config_network()
client = ValidatorClient(network.validator_config)
tx = client.post.transfer(
    subaccount=subaccount,
    recipient_address=subaccount.address,
    recipient_subaccount_number=1,
    asset_id=0,
    amount=5_000_000,
)
```

```typescript [TypeScript]
const client = new CompositeClient(network);
const tx = await client.transferToSubaccount(
    subaccount,
    destinationAddress,
    destinationSubaccountNumber,
    parseFloat(amount).toFixed(6),
    Method.BroadcastTxCommit,
);
```

:::

Examples: [Python] | [TypeScript] | [Rust]

[Python]: https://github.com/dydxprotocol/v4-clients/blob/main/v4-client-py-v2/examples/transfer_example_transfer.py
[TypeScript]: https://github.com/dydxprotocol/v4-clients/blob/main/v4-client-js/examples/transfer_example_subaccount_transfer.ts
[Rust]: https://github.com/dydxprotocol/v4-clients/blob/main/v4-client-rs/client/examples/transfer.rs
