### Send Token

Transfer a token asset from one address to another one.

#### Method Declaration

:::code-group

```python [Python]
async def send_token(
    self,
    wallet: Wallet,
    sender: str,
    recipient: str,
    quantums: int,
    denomination: str,
)
```

```typescript [TypeScript]
async sendToken(
    subaccount: SubaccountInfo,
    recipient: string,
    coinDenom: string,
    quantums: string,
    zeroFee: boolean = true,
    broadcastMode?: BroadcastMode,
): Promise<BroadcastTxAsyncResponse | BroadcastTxSyncResponse | IndexedTx>
```

```rust [Rust]
pub async fn send_token(
    &mut self,
    account: &mut Account,
    sender: Address,
    recipient: Address,
    token: impl Tokenized,
) -> Result<TxHash, NodeError>
```

```url [API]
```

:::

:::details[Unification Plan]
:::

#### Parameters

| Parameter         | Location  | Type      | Description                                       |
| ----------------- | --------- | --------- | ------------------------------------------------- |
| `wallet`          | query     | [Wallet]  | The wallet to use for signing the transaction.    |
| `sender`          | query     | string    | The sender address.                               |
| `recipient`       | query     | string    | The recipient address.                            |
| `quantums`        | query     | int       | The amount of quantums to send.                   |
| `denomination`    | query     | int       | The denomination of the token.                    |

#### Response

| Status | Meaning     | Schema |
| ------ | ----------- | ------ |
| `200`  | [OK]        | string |

Examples: [Rust]

[Rust]: https://github.com/dydxprotocol/v4-clients/blob/main/v4-client-rs/client/examples/send_token.rs

[Wallet]: /types/wallet
